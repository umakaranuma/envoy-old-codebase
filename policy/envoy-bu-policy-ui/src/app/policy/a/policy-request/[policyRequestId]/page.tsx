import React from 'react';
import { UpdatePolicyInfo } from '../_utils/components/UpdatePolicyInfo';

async function page() {
  // const searchParamsResolved = await searchParams;
  // const issuedPolicyId = searchParamsResolved.ipId;
  // if (issuedPolicyId) {
  //   return <UpdatePolicyInfo />;
  // } else {
  return <UpdatePolicyInfo />;
  // }
}

export default page;
